export default class Student {
    constructor(id, name, password, rootFolder) {
        this.id = id
        this.name = name
        this.password = password
        this.rootFolder = rootFolder
    }

}